{
  "name": "ai-starter",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "tests"
  ],
  "scripts": {
    "db:studio": "bunx drizzle-kit studio",
    "db:push": "bunx drizzle-kit push",
    "cli": "bun apps/cli/index.ts",
    "api": "bun apps/api/src/index.ts",
    "test": "bun test",
    "eval:dev": "evalite watch",
    "eval": "evalite",
    "typecheck": "tsc --build packages/*/tsconfig.json apps/*/tsconfig.json tests/tsconfig.json",
    "lint": "eslint . && prettier --check .",
    "lint:fix": "bun format && eslint --fix --unsafe .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "bun test && bun run eval && bun run typecheck && bun run lint"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "better-sqlite3": "^12.4.6",
    "drizzle-kit": "^0.31.6",
    "eslint": "^9.39.1",
    "evalite": "^1.0.0-beta.15",
    "prettier": "^3.2.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.4",
    "vitest": "^4.0.14"
  }
}
